---
"@kilocode/cli": minor
---

Report a distinct `scheduled` session status while a session sleeps until its scheduled wake, carrying the wake instant as `scheduledAt`, and show the state and the wake time in `kilo session list`.
